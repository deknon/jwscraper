# Upload signing key

`saha-upload.jks` is the shared key used to sign GitHub release APKs so updates
install over previous versions across Cloud Agent VMs.

Do not replace this file casually — a new key forces users to uninstall first.
